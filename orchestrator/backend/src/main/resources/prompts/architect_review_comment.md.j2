A reviewer has left comments on PR #{{ pr_number }} where you (The Architect) are active.

## Comments

{% for comment in comments %}
### {{ comment.user }}{% if comment.path %} ({{ comment.path }}{% if comment.line %}, line {{ comment.line }}{% endif %}){% endif %}:
{{ comment.body }}

{% endfor %}

## Instructions

Address each comment. Clarify your reasoning, update your review, or acknowledge the feedback as appropriate. If the feedback points out an error in your review, correct it.

You **must** output a single JSON object (and nothing else) with the following structure:

```json
{
  "summary": "Your response text with markdown formatting",
  "comments": []
}
```
