You are implementing issue {{ issue_ref }}: {{ issue_title }}

## Plan

Read the development plan at `{{ plan_file_path }}` and implement it.

{% if attachments %}

## Attachments

The following files were attached to this issue and are available in the container:
{% for path in attachments %}
- `{{ path }}`
{% endfor %}

You may read these files directly or copy them into the repository if needed.
{% endif %}
{% if designs %}

## Designs

These Figma frames were linked from the issue and rendered into the container:
{% for design in designs %}
- `{{ design.path }}` — frame "{{ design.frame }}"{% if design.file %} in "{{ design.file }}"{% endif %}
  ({{ design.url }})
{% endfor %}

Read the images. A design usually settles layout, states and wording the issue
text leaves open; where it disagrees with the text, say so rather than guessing.
{% endif %}

## How you are running

You work unattended: a tool call outside this turn's allowlist is denied automatically, and nobody can approve the prompt — never ask for permissions, mode switches, or prompt approvals. You have write access to the workspace for this turn. If you need another repository purely as reference, clone it inside the workspace at `.smithy/tmp/` (it is excluded from git); paths outside the workspace are not readable. If something outside your reach genuinely blocks a part of the plan, implement what you can and state plainly in your final report what was blocked and why.

## Instructions

1. Read the plan file carefully.
2. Implement each step described in the plan.
3. Add or update tests covering your changes as described in the plan's Testing section, then run the test suite to verify your implementation.
4. **You MUST `git add` and `git commit` all your changes before finishing.** Do not leave any uncommitted work. Use one or more commits as appropriate.

Do NOT reference the issue number (e.g. `{{ issue_ref }}`) in commit messages — the commits are visible in the PR, and issue references clutter the issue timeline. Do NOT push — the orchestrator handles pushing.
