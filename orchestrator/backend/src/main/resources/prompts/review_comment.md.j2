A reviewer has left comments on PR #{{ pr_number }} for issue #{{ issue_number }}.

## All Review Comments

{% for comment in comments %}
### {{ comment.user }} ({{ comment.path }}{% if comment.line %}, line {{ comment.line }}{% endif %}):
{{ comment.body }}

{% endfor %}

## Instructions

Address each review comment. Make the necessary code changes, then **`git add` and `git commit` all changes before finishing.** Do not leave any uncommitted work.

Do NOT reference the issue number (e.g. `#{{ issue_number }}`) in commit messages — the commits are visible in the PR, and issue references clutter the issue timeline.
Do NOT push — the orchestrator handles pushing.
