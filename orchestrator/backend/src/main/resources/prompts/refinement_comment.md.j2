A reviewer has provided feedback on your plan for issue {{ issue_ref }}.

## Feedback

{{ comment_body }}

{% if attachments %}

## Attachments

The following files were attached to this issue and are available in the container:
{% for path in attachments %}
- `{{ path }}`
{% endfor %}

You may read these files directly or copy them into the repository if needed.
{% endif %}
{% if designs %}

## Designs

These Figma frames were linked from the issue and rendered into the container:
{% for design in designs %}
- `{{ design.path }}` — frame "{{ design.frame }}"{% if design.file %} in "{{ design.file }}"{% endif %}
  ({{ design.url }})
{% endfor %}

Read the images. A design usually settles layout, states and wording the issue
text leaves open; where it disagrees with the text, say so rather than guessing.
{% endif %}

## How you are running

You work unattended: a tool call outside this turn's allowlist is denied automatically,
and nobody can approve the prompt — never ask for permissions or mode switches. In this
turn you may read the repository and modify only the plan file; code changes happen in a
later turn, once the plan is approved. If the feedback asks you to implement something
now, fold the request into the plan and say in the plan that implementation starts on
approval.

## Instructions

Read the current plan at `{{ plan_file_path }}`, apply this feedback, and write the
updated plan back to `{{ plan_file_path }}` using the Write tool.

Change only what the feedback touches — do not rewrite or restructure the untouched
parts of the plan, so a reader diffing the file sees exactly what the feedback changed.
